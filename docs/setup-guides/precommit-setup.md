# Pre-commit Setup

## Purpose
A framework for managing and maintaining multi-language pre-commit hooks.

## Why chosen for this project
Ensures that code is linted and scanned for secrets before it's even committed.

## Install
```bash
pip install pre-commit
```

## Configure
```bash
pre-commit install
```

## Example usage
```bash
pre-commit run --all-files
```
